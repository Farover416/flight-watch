fast-flights==3.1.0
# fast-flights imports typing_extensions but does not declare it
typing_extensions>=4.0
# pulled in by fast-flights, but we parse the page ourselves now, so ask for it
selectolax>=0.3
# Only for the handful of price checks that need a real browser. The sweep
# still runs without it - verification is skipped and says so.
playwright>=1.40
PyYAML>=6.0
